#MYSQL configuration
spring.datasource.url=jdbc:mysql://${MYSQLHOST:containers-us-west-26.railway.app}:${MYSQLPORT:5623}/${MYSQLDATABASE:railway}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:Zu2N7vvRynrTHwxGavn7}
#to see queries executing in console
spring.jpa.show-sql=true
#updates the data instead of recreating it every time to not lose all of our data
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect